// Define an enumeration named OutputFormat to represent different output formats.
package com.example.pokedex.utilities;

// Declare the OutputFormat enum with three constant values: TEXT, HTML, and CSV.
public enum OutputFormat {
    TEXT,    // Represents the human-readable text format.
    HTML,    // Represents the HTML format.
    CSV      // Represents the CSV format.
}
